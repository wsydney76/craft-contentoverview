# Using VitePress

Run `ddev npm install` to install the dependencies.

The URLs reported by npm scripts won't work in DDEV environment.

Use `http://contentoverview.ddev.site:5173/craft-contentoverview/` instead.