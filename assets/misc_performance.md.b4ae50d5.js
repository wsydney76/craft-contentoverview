import{_ as e,c as a,o as i,a as t}from"./app.6b7e9cc0.js";const p=JSON.parse('{"title":"Performance","description":"","frontmatter":{},"headers":[],"relativePath":"misc/performance.md"}'),o={name:"misc/performance.md"},n=t('<h1 id="performance" tabindex="-1">Performance <a class="header-anchor" href="#performance" aria-hidden="true">#</a></h1><p>Response time is highly dependent on your individual config, so here are just a few notes:</p><ul><li>Obviously, more things on a page will make it slower, leading to more queries/image transforms (Sorry for this trivial statement...).</li><li>So it is all about finding a balance.</li><li>We found that people prefer a longer initial load time and having anything they need available from the start, rather than jumping around between different pages.</li><li>Better hosting always pays off.</li><li>By default, all content on a page is rendered server side (including all tabs).</li><li>Set the <code>loadSectionsAsync</code> plugin setting to <code>true</code> if the section html shall be loaded by ajax calls. So section will only be loaded if the section is in the viewport.</li><li>Set the <code>showLoadingIndicator</code> plugin setting to <code>true</code> if you want a visual clue while an ajax request is running.</li><li>Because single request will likely be fast, this can be somewhat confusing.</li><li>Transformed images are generated on the fly if they don&#39;t already exist, so a lower <code>limit</code> can speed up things.</li><li>Images and their transforms are automatically eager loaded if defined via <code>imageField</code> or <code>fallbackImageField</code>.</li><li>If you want to eager load other related elements, use the <code>Section::EVENT_MODIFY_CONTENTOVERVIEW_QUERY</code> event.</li><li>You can populate the <code>custom</code> setting in your config with any data in advance.</li><li>You can overwrite any class if you need some special handling.</li></ul>',3),l=[n];function r(s,d,c,h,f,u){return i(),a("div",null,l)}const g=e(o,[["render",r]]);export{p as __pageData,g as default};
